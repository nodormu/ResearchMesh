[project]
name = "linux-cli-research-client-for-claude"
version = "0.1.0"
description = "Claude Research Assistant for Linux CLI with extended tools and functions."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.51.0",
    "mcp[cli]>=1.8.0",
    "prompt-toolkit>=3.0.51",
    "pydantic>=2.0",
    "playwright>=1.40.0",
]
