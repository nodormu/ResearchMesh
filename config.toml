# Linux CLI Research Client for Claude — configuration.
#
# Non-secret settings only. The n8n Bearer token is NOT stored here — it stays
# in the N8N_MCP_TOKEN environment variable (see README.md).

[claude]
# Anthropic model to use. The CLAUDE_MODEL environment variable overrides this.
model = "claude-sonnet-5"

[n8n]
# Set enabled = false to skip connecting to the n8n MCP server entirely (e.g.
# when it's turned off). The app still runs with its local tools.
enabled = false # set true if your n8n/mcp server is reachable/turned on

# n8n MCP Server Trigger endpoint (Streamable HTTP transport).
# The N8N_MCP_URL environment variable, if set, overrides this value.
url = "http://192.168.2.12:5678/mcp-server/http"
